softmax回归就是一个单层的神经网络

词袋模型只考虑某个字，每句话为一个向量，长度为所有字的数量，如果字典第n个字出现，向量第n位为1，否则为0
n-gram N元特征考虑相邻n个字组成的词，长度为字+词的数量，编码方式同理

激活函数：输出进入下一层神经元前的非线性处理

损失函数：判断预测与真实值的差距
+ 交叉熵、均方差、平均绝对误差

batch：单次更新参数所用的样本集合，batch-size为集合大小
epoch：使用所有样本进行一次训练

